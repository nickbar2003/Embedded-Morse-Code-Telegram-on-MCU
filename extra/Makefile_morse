default:
		avr-gcc -Os -DF_CPU=16000000UL -mmcu=atmega328p -c -o morse.o morse.c
		avr-gcc -o morse.bin morse.o
		avr-objcopy -O ihex -R .eeprom morse.bin morse.hex
		sudo avrdude -F -V -c arduino -p ATMEGA328P -P /dev/ttyACM0 -b 115200 -U flash:w:morse.hex
